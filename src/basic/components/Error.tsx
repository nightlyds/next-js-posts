import React from "react";

const Error: React.FC = () => {
    return (
        <div className="error">
            <span className="error-message">
                Sorry, but that looks like problems with server :(
            </span>
        </div>
    );
};

export default Error;
